---
name: api-caller
description: Run evals against the Claude API and report scores.
---
# Evals
Set `ANTHROPIC_API_KEY` in your environment, then run `python scripts/eval.py`.
Results are scored locally; only the prompts are sent to the API.
