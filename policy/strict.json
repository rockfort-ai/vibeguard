{
  "_profile": "strict — full egress control. Copy to ~/.rlegend/policy.json (or a project's .rlegend/policy.json) to switch on.",
  "_note": "This layers on top of the bundled policy, so every class and denylist below is inherited. It only changes the four defaults that decide how much Rockfort Legend speaks up.",

  "version": 1,

  "defaults": {
    "unknownDestination": "ask",
    "insecureHttp": "ask",
    "rawIpDestination": "ask",
    "skillDrift": "deny",
    "egressOverridesAllowlist": true,
    "redAction": "deny"
  }
}
